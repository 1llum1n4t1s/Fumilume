# Fumilume 設計

## 目的と範囲

Fumilume は、複数のテキスト文書を垂直タブで扱う Windows 専用エディタです。文字コードと改行コードを維持した編集、Markdownプレビュー、PDF閲覧、サクラエディタを参考にした編集操作、設定・更新・ファイル関連付けを単一のデスクトップアプリとして提供します。

対象は `net10.0-windows10.0.26100.0`、最低対応OSは Windows 10 バージョン1809です。配布物は x64 / ARM64 の自己完結型Native AOTアプリです。

## システム構成

| 領域 | 主な実装 | 責務 |
| --- | --- | --- |
| 起動・ライフサイクル | `Program.cs`, `App.axaml.cs` | ログ初期化、Velopackコールバック、設定読込、テーマ適用、メインウィンドウ生成 |
| View | `Views/`, `Styles/`, `App.axaml` | Avalonia UI、エディタとミニマップの接続、Markdown描画、PDF画像表示、テーマ別リソース |
| ワークスペース | `MainWindowViewModel`, `WorkspaceTabViewModel` | タブ集合、選択状態、ファイル操作、保存確認、コマンド可否、設定タブ管理 |
| 文書モデル | `DocumentViewModel`, `DocumentBookmarks` | AvaloniaEdit文書、変更状態、カーソル、検索・編集・変換、Markdown表示状態 |
| PDFモデル | `PdfDocumentViewModel` | PDFの読込、ページ範囲、拡大率、非同期レンダリング、画像ライフタイム |
| 設定モデル | `AppOptionsViewModel`, `SettingsTabViewModel` | UIへ公開する設定、値の即時反映、永続化、関連付け操作 |
| サービス | `Services/` | 文書I/O、設定、ログ、更新、ダイアログ、テーマ、関連付け、Markdown解析、PDFレンダリング、フォルダ横断検索、構文ハイライト、アウトライン解析、キーボードマクロ |
| 配布 | `scripts/release-local.ps1`, `web/` | 署名付きVelopack成果物、R2配信、更新マニフェスト、ランディングページ |

ViewModel は状態と操作を持ち、View は表示とAvalonia固有の接続を担当します。ファイルシステム、Windows API、更新UIなどの副作用はサービスへ分離し、ViewModelテストから代替実装を渡せる境界を保ちます。

## タブと状態モデル

`MainWindowViewModel.Tabs` は `WorkspaceTabViewModel` の集合で、次の4種類を扱います。

- `DocumentViewModel`: 編集可能なテキスト文書。未保存状態、パス、文字コード、改行、カーソル、Markdownプレビュー状態を保持する。
- `PdfDocumentViewModel`: 読み取り専用PDF。ページと表示画像を保持し、編集・保存コマンドの対象外になる。
- `GrepResultTabViewModel`: フォルダ横断検索の結果。検索条件と一致行を保持し、選んだ行から該当ファイルを開く。
- `SettingsTabViewModel`: 単一の設定画面。重複して開かず、常に文書・PDFタブの後ろへ置く。

選択タブの型から、エディタ、Markdownプレビュー、PDFビュー、検索結果、設定ビューの表示とコマンド可否を導出します。表示可能な文書・PDF・検索結果が0件になった場合は空の文書を作り、設定タブだけの状態を残しません。

## 主要データフロー

### 起動

1. `Program` が `AppLogger` を初期化し、未処理例外を記録する。
2. Velopackがインストール・更新後の関連付け更新と、アンインストール前の関連付け解除を処理する。
3. `App` が `SettingsService` から設定を1回読み、初回描画前に `ThemeService` へ適用する。
4. `MainWindow` と `MainWindowViewModel` が作られ、起動引数のファイルをワークスペースへ開く。

### テキストの読込・編集・保存

1. `MainWindowViewModel` がダイアログまたは起動引数から絶対パスを受け取る。
2. `DocumentFileService` がBOMと厳密なUTF-8判定から文字コードを決め、最初に現れる改行形式を記録する。利用者がステータスバーから選び直した場合は、その値を文書の保存メタデータへ反映する。
3. `TextDocumentContent` を `DocumentViewModel` へ読み込み、AvaloniaEditの文書を編集元データとする。
4. 保存時は文書が現在保持する文字コードと改行へ正規化し、同じディレクトリの一時ファイルから置き換える。設定が有効な場合だけ置換前の内容を `.bak` に残す。
5. 保存・クローズ時に、設定が有効ならファイルごとのカーソル位置を記録する。

### Markdown

`.md` の `DocumentViewModel` だけがプレビューへ切り替えられます。`MarkdownDocumentParser` が編集元テキストを表示用ブロックへ変換し、`MarkdownPreview` がAvaloniaコントロールとして描画します。プレビューは派生表示であり、保存される正本は常にエディタ内のMarkdown文字列です。

### PDF

`.pdf` は `WindowsPdfRenderer` が `Windows.Data.Pdf` で開き、要求ページをビットマップへ変換します。`PdfDocumentViewModel` はページ移動と25～400%の拡大率を管理し、古いレンダリング結果とストリームを破棄します。PDFは閲覧専用で、テキスト抽出・編集・保存は責務に含めません。

### 設定

設定Viewの操作は `AppOptionsViewModel` を経て共有中の `AppSettings` を更新し、テーマ・フォント・エディタ・タブへ即時反映します。IBM Plex Sans JP と UDEV Gothic JPDOC は `FumilumeFontCollection` からアプリ同梱リソースとして解決し、各々をUI・エディタの既定値にします。`SettingsService` は未知・破損JSONを既定値へフォールバックし、数値や記録件数を許容範囲へ補正します。保存は `AtomicFile` により、同一ディレクトリの一時ファイルを書き切ってから置き換えます。

### 機能の入口

操作の入口は 3 層に分けます。常時出すツールバーは「保存」「元に戻す」「やり直す」「検索」の 4 つと、文脈で出る Markdown プレビューだけにし、`EditorCommandCatalog` の 50 件は編集画面の右クリックメニュー（区分ごとのサブメニュー）から辿ります。すべての機能はコマンドパレット（`Ctrl+Shift+P`）に `CommandPaletteEntry` として並び、ファイルを開く・保存するといったワークスペース操作も同じ一覧へ載せて、今の状態で実行できるものだけを出します。

機能を足すたびにツールバーへ並べると横に伸びて優先度が消え、同じ機能へ最大 4 つの入口ができていたため、増える先をパレットとメニューに固定しています。

### 左サイドパネルとアウトライン

左側は `SidePanelKind`（タブ一覧・アウトライン・ブックマーク）を切り替える 1 枚の面です。幅は `GridSplitter` で216～480pxの範囲を変更でき、中央領域は最低480pxを保ちます。新規・開く・設定は一覧下端ではなく、パネル切り替えと同じ上部へまとめます。`MainWindowViewModel` は開いている面だけを引き直し、閉じている面は次に開くまで解析しません。中身が前回と同じときは差し替えないので、打鍵のたびに選択と表示位置が飛びません。ブックマークは選択中の文書の `DocumentBookmarks.Changed` を購読し、タブを切り替えるたびに購読先を張り替えます。

フォルダ横断検索の結果はここへ入れず、中央のタブに残します。1 行 1 一致で「場所」と「本文」を横に並べる一覧なので、可変幅の縦列へ押し込まず中央の表示幅を使います。

### エディタのミニマップ

`EditorMinimap` は AvaloniaEdit の文書とスクロール状態を読み、行をサンプリングした縮小図と現在の表示範囲を描きます。文書そのものは複製せず、長い文書では描画行数を表示ピクセル数まで抑えます。クリックとドラッグは `IScrollable.Offset` へ変換してエディタを移動し、Markdownプレビュー中は表示しません。

`OutlineService` は Markdown の見出し（ATX と Setext、フェンス内と YAML 前書きは除く）と、波括弧で入れ子を作る言語の型・メンバー宣言だけを拾い、判定できない拡張子では空を返します。後者は構文解析ではなく行単位の照合で、注釈と文字列の中身を落としてから波括弧を数えて入れ子の深さを決めます。制御構文や呼び出しを宣言と読み違えないよう、宣言の手前に語が無い行と、`throw` や `await` などの語を含む行は落とします。中途半端に拾った一覧は「ここに無いなら本文にも無い」と誤読させるため、拾えることより誤って拾わないことを優先します。

### フォルダ横断検索

`GrepService` が指定フォルダのファイルを列挙し、`IDocumentFileService` を通して読み、行単位で照合します。読み込み経路を文書と共有するのは、対応する文字コードの判定を開いたときと一致させるためです。文字として読めないファイル（先頭に NUL を含むもの。ただし UTF-16 の BOM が付くものは除く）、16MB を超えるファイル、読み取りに失敗したファイルは数えたうえで飛ばし、1 件の失敗で検索全体を止めません。一致は5000件で打ち切り、打ち切りと除外の件数を結果へ添えます。

結果は `GrepResultTabViewModel` として文書・PDF・設定と同じ縦タブへ並べます。照合は `Task.Run` で UI スレッドから外し、行を選ぶと `MainWindowViewModel` が該当ファイルを開いて（既に開いていればそのタブを選び）、一致した桁へカーソルを置いて行全体を選択します。

### 構文ハイライト

`SyntaxHighlightingService` が拡張子から AvaloniaEdit 同梱の定義を選びます。同梱の配色はライトテーマ前提で暗い色を含むため、ダークでは前景色の明度だけを持ち上げます。書き換えは共有インスタンスへ及ぶので、元の色を参照比較の辞書へ控え、毎回そこから計算し直してテーマの往復で色が積み上がらないようにします。全定義をまとめて処理しないのは、同梱の定義に単体では読み込めない部品（他の定義から参照される断片）が含まれるためです。

### セッションの復元

終了時のワークスペースは `MainWindowViewModel` が `SessionState` へ写し、`SessionStateService` が `session.json` と、未保存の本文だけを持つ控えファイルへ分けて書きます。本文を設定と同居させないのは、設定が小さく頻繁に読み書きする正本であり、数MBの本文が混ざると保存コストと破損時の被害が設定全体へ広がるためです。控えは保存のたびに新しい名前で書き、一覧を確定させたあとにだけ古い控えを消します。同じ名前へ上書きすると、一覧の書き込みが失敗したときに前回の一覧が今回の本文を指し、書きかけが別タブの内容へすり替わるためです。

復元では、未保存の変更があるタブは控えた本文を正本にしてディスクを読まず、変更が無いタブはディスクを読み直し、実体が消えたタブは復元しません。控えを読めなかった未保存タブは、保存済みファイルならディスクの内容で開いて失った旨を伝え、新規文書なら復元しません。タブ構築の途中で例外が起きた場合は、その未保存タブと控えへの参照を次のセッション一覧にも残し、一時的な復元失敗を終了時の掃除でデータ消失へ変えません。ディスクから読み直す文書とPDFには、通常の「開く」と同じ大容量ファイル確認を適用します。

終了時は `PersistSessionStateAsync` が起動時の復元完了を待ってから書き直します。復元の途中で書くと、まだ戻していないタブの控えを未使用と判断して消してしまうためです。復元が始まる前の保存要求では前回セッションを正本として触りません。通常のウィンドウ終了では、設定 `RestoreSession` が有効な間は未保存の確認を出さない代わりに、控えへ書けなかったときは未保存文書がある限り終了を中止します。OSまたはアプリ終了ではダイアログと非同期待機を挟まず、現在確定しているタブ状態を同期的に控えてから終了処理へ戻ります。無効なときは控えを削除して従来の確認へ戻します。

アプリは `SingleInstanceCoordinator` がユーザーデータ置き場の排他リースを保持し、同じユーザーによる後続起動の引数を名前付きパイプで最初のプロセスへ渡します。相対パスは送信側プロセスの作業フォルダーで絶対化し、受信側の作業フォルダーで別の実体へ解決されないようにします。転送されたファイルは初回セッション復元の完了後に開くため、ファイル関連付けから何度起動しても、複数プロセスが同じ `session.json` と未保存バッファを競合更新しません。

### キーボードマクロ

記録するのはキー入力そのものではなく、それが意味する操作（`MacroStep`）です。文字入力とカーソル移動・削除は View が `TextArea` の入力から翻訳し、`EditorCommandCatalog` のコマンドは `RunEditorCommandAsync` の分岐 1 箇所で 記録します。再生は入力を流し直さず、`DocumentViewModel` へ同じ操作を当て直します。

入力を溜めて再送する作りにしないのは、IME・フォーカス・タイミングに依存して不安定になり、ヘッドレスのテストで挙動を固定できず、キーイベントは環境依存で保存もできないためです。代わりにマウスでのカーソル移動は記録できません。

1 回の実行は `UndoStack` のひとつの塊にまとめます。100 回流したマクロを `Ctrl+Z` 100 回で戻すことになると、取り消せないのと変わらないためです。再生中は記録を止めます（記録中に実行できてしまうと、マクロが自分自身を書き足して際限なく伸びます）。入力ダイアログを開くコマンド（`GoToLine`、`BookmarkPattern`）は再生時に止まるので、再生側で読み飛ばすのではなく記録の時点で弾き、一覧に「動かない手」を残しません。

検索の手は AvaloniaEdit の `SearchPanel` を参照せず、記録した時点の条件（文字列・大文字小文字・正規表現）を手自身が持ち回り、再生では `DocumentViewModel.FindNext` がモデル側で探します。View の状態に依存させるとモデル層だけで再生できなくなり、テストで固定できなくなるためです。

### 設定画面の検索

設定は 6 タブ 20 枚のカードにまたがるため、`SettingsView` が読み込み時にカード単位の索引を作り、検索語に当たったカードとタブだけを残します。走査に視覚ツリーを使わないのは、`TabControl` が選んでいないタブの中身を組み立てないためで、`SettingsSearch` は XAML の読み込み時点で埋まっている `Content` と `Children` だけを辿ります。照合対象には項目名だけでなく補足文も含めるので、機能の呼び名を知らなくても説明の言葉で辿り着けます。

### 更新と配布

アプリ側の `UpdateService` は `VelopackUpdateDialog.Avalonia` を介して更新確認・ダウンロード・適用・再起動を行います。配布側は `scripts/release-local.ps1` が x64 / ARM64 を順番にNative AOT発行し、コード署名、Velopackパッケージ化、Cloudflare R2へのアップロード、固定URLのキャッシュパージ、公開ファイルの版・ハッシュ・サイズ・署名照合を行います。

## 永続データと外部境界

- `%LocalAppData%\Fumilume\settings.json`: 利用者設定とカーソル位置
- `%LocalAppData%\Fumilume\session.json` と `%LocalAppData%\Fumilume\session`: 前回終了時のタブ構成と未保存の本文
- `%LocalAppData%\Fumilume\macros.json`: 名前を付けて保存したキーボードマクロ
- `%LocalAppData%\Fumilume\logs`: `SuperLightLogger` による日別ログ
- Windowsユーザー別レジストリ: 対応拡張子のファイル関連付け
- `fumilume.kagayoi.com`: ランディングページ、固定セットアップURL、更新マニフェストとパッケージ
- Cloudflare R2 `fumilume-updates`: 配布成果物の正本

テストでは `AppStoragePaths` の保存先を一時ディレクトリへ差し替え、実利用者の設定を変更しません。署名証明書とCloudflare認証情報はリポジトリ外に置き、製品コードやテストデータへ取り込みません。

## 重要な不変条件

- テキスト文書は、利用者が選び直さない限り、対応範囲内で読込時の文字コードと改行コードを保存後も維持する。
- 行番号・アウトライン・Markdown解析・行単位編集は、CRLF・LF・CRを同じ行区切りとして扱う。
- 未保存文書のタブを個別に閉じる前に、保存・破棄・取消の決定を完了する。アプリ終了時は、セッション復元が有効なら未保存の内容を控えへ引き継ぎ、無効なら同じ決定を完了する。
- セッションを控える設定では、未保存の本文を控えへ書けたことを確認してから終了する。書けない場合は終了しない。
- セッションの控えは、新しい一覧を確定させたあとにだけ古い版を消す。確定前に既存の控えを上書きしない。
- 同じユーザーの通常起動は単一プロセスへ集約し、後続起動のファイル引数は初回復元後の既存ウィンドウへ渡す。
- 設定タブは最大1個で末尾にあり、コンテンツタブが0個の状態を作らない。
- フォルダ横断検索の結果は中央のタブに出し、左サイドパネルへ移さない。
- 常時出すツールバーの操作は増やさない。機能を足すときの入口はコマンドパレットと右クリックメニューにする。
- アウトラインは対応する拡張子だけを解析し、判定できない形式では空を返す。推測で見出しを作らない。
- マクロの再生中は記録しない。1 回の実行は回数によらずひとつの取り消し単位にする。
- 入力を尋ねるコマンドはマクロに記録しない。再生を止める手を一覧へ残さない。
- PDF選択中はテキスト編集・保存操作を有効にしない。
- Markdownプレビューは `.md` だけに提供し、編集元テキストを変更しない。
- UIフォントとエディタフォントは独立し、エディタ候補は等幅フォントだけにする。
- 設定保存の失敗で既存の正常な設定ファイルを途中内容へ置き換えない。
- x64とARM64の成果物は同じアプリ版を持ち、それぞれ対応する更新チャンネルへ公開する。

## 採用済みの設計判断

- **Avalonia + AvaloniaEdit**: Windowsのネイティブデスクトップ体験と高度な編集基盤を得る。Windows API利用とNative AOTを優先するため、現在はクロスプラットフォーム化を目的にしない。
- **MVVMとサービス境界**: タブ・コマンド状態をViewModelで検証し、ファイルやOS依存処理を差し替え可能にする。Avalonia固有の描画・入力接続だけはView側へ残す。
- **Windows.Data.Pdf**: 追加PDFランタイムを同梱せずWindows標準機能で描画できる。一方でWindows専用となり、閲覧以外のPDF機能は持たない。
- **WebViewを使わないMarkdown表示**: 外部ブラウザランタイムやHTML実行を避け、テーマと入力境界をAvalonia内で完結させる。対応構文はパーサーとViewが実装する範囲に限られる。
- **原子的なローカル保存**: 一時ファイルと置き換えでクラッシュ時の破損を抑える。文書の `.bak` は利用者設定で明示的に有効化した場合だけ作る。
- **自己完結Native AOT配布**: .NETランタイム不要の起動と配布を優先する。RIDごとの発行・署名・検証が必要になるため、リリース処理は逐次実行する。
- **マクロは操作を記録し、入力は記録しない**: モデル層だけで再生でき、ヘッドレスのテストで固定でき、環境をまたいで保存できる。引き換えにマウス操作とクリップボードは記録の対象外になる。
- **共有更新・ログライブラリの薄い統合**: 更新UIとログ出力を共通ライブラリへ集約し、Fumilume固有コードは起動、設定、エラー表示の接着に限定する。

実装変更時の必須コマンドと作業規約は [AGENTS.md](AGENTS.md)、利用者から見える現在の機能は [README.md](README.md) を正本とします。
